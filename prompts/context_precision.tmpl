You are evaluating whether retrieved documents are actually relevant to a
user's question.

USER INPUT:
{{.Input}}

RETRIEVED DOCUMENTS (in retrieval order):
{{range $i, $doc := .Context}}[{{$i}}] {{$doc}}
{{end}}

Instructions:
1. Rate each document's relevance to USER INPUT from 0.0 (irrelevant) to 1.0 (highly relevant).
2. Compute a rank-weighted mean: earlier documents weigh more.
   Weighted score = sum(relevance[i] / (i+1)) / sum(1 / (i+1)) for i in 0..n-1.
3. Score = weighted mean. If RETRIEVED DOCUMENTS is empty, score 0.0.

Respond with a single JSON object and nothing else:
{"score": <float 0.0-1.0>, "reason": "<one-sentence explanation>"}
